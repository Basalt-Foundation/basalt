namespace Basalt.Network;

// Network module implemented — see PeerManager, GossipService, Messages, PeerId, PeerInfo.
public static class NetworkPlaceholder
{
    public static void Configure()
    {
    }
}
